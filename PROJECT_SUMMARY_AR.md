# ملخص مشروع منصة الصحة الرقمية - Health Poster Legend
## Project Summary for Gemini AI Partner

**التاريخ**: 2024  
**الإصدار**: 1.0.0  
**الحالة**: ✅ Production Ready

---

## 📋 نظرة عامة على المشروع (Project Overview)

### الهدف الرئيسي
منصة رقمية متكاملة لإدارة إحصائيات المراكز الصحية في محافظة كركوك، العراق. تتيح المنصة للمراكز الصحية إرسال تقارير شهرية، وتمكن إدارة القطاع من مراجعة واعتماد هذه التقارير، بالإضافة إلى إنشاء بوسترات توعوية احترافية.

### نوع المشروع
- **نوع التطبيق**: Progressive Web App (PWA)
- **المنصة**: Web Application (يعمل على جميع الأجهزة)
- **اللغة الأساسية**: العربية (RTL Support)
- **الجمهور المستهدف**: 
  - موظفو المراكز الصحية (center_user)
  - إدارة القطاع الصحي (admin)

---

## 🏗️ البنية التقنية (Technical Architecture)

### Stack التقني

#### Frontend Framework
- **Next.js 16.1.1** (React 19.2.3)
- **TypeScript 5**
- **Tailwind CSS 4** (PostCSS)
- **App Router** (Next.js App Directory)

#### Backend & Database
- **Supabase** (PostgreSQL Database)
  - Authentication (Email/Password)
  - Row Level Security (RLS)
  - Real-time subscriptions
  - Storage (للمستقبل)

#### المكتبات الرئيسية
- **@supabase/supabase-js**: للاتصال بقاعدة البيانات
- **recharts**: للرسوم البيانية والتحليلات
- **xlsx**: لمعالجة ملفات Excel
- **html2canvas & html-to-image**: لتصدير البوسترات
- **jspdf**: لإنشاء ملفات PDF

---

## 📁 هيكل المشروع (Project Structure)

```
health-poster-legend/
├── src/
│   ├── app/                    # صفحات Next.js (App Router)
│   │   ├── admin/             # صفحات الإدارة
│   │   │   ├── approvals/      # صفحة الموافقات
│   │   │   ├── audit-log/      # سجل التدقيق
│   │   │   ├── presentation/   # عرض تقديمي تنفيذي (PDF)
│   │   │   ├── reports/        # التقارير
│   │   │   └── user-guide/     # دليل المستخدم (PDF)
│   │   ├── login/              # تسجيل الدخول
│   │   ├── signup/             # التسجيل
│   │   ├── statistics/         # إرسال الإحصائيات (للمراكز)
│   │   ├── sector-dashboard/   # لوحة القطاع (للإدارة)
│   │   ├── poster/             # إنشاء البوسترات
│   │   ├── pending-approval/   # انتظار الموافقة
│   │   ├── layout.tsx          # التخطيط الرئيسي
│   │   └── page.tsx            # الصفحة الرئيسية
│   │
│   ├── components/             # المكونات القابلة لإعادة الاستخدام
│   │   ├── layout/             # مكونات التخطيط
│   │   │   ├── PageContainer.tsx
│   │   │   ├── PageHeader.tsx
│   │   │   └── SectionCard.tsx
│   │   ├── poster/             # مكونات البوستر
│   │   │   ├── PosterEditor.tsx
│   │   │   ├── PosterExportActions.tsx
│   │   │   ├── PosterHistory.tsx
│   │   │   ├── PosterPreview.tsx
│   │   │   └── PosterTemplateSelector.tsx
│   │   ├── BottomNav.tsx       # التنقل السفلي (Mobile)
│   │   ├── InsightsPanel.tsx   # لوحة الرؤى الذكية
│   │   ├── LoadingSpinner.tsx
│   │   ├── ProtectedRoute.tsx  # حماية الصفحات
│   │   ├── PWAInstallPrompt.tsx
│   │   ├── SkeletonLoader.tsx
│   │   ├── StatisticsWizard.tsx
│   │   ├── StatusBadge.tsx     # شارة الحالة
│   │   └── StatusTimeline.tsx
│   │
│   ├── contexts/               # React Contexts
│   │   └── AuthContext.tsx     # إدارة المصادقة
│   │
│   └── lib/                    # المكتبات والوظائف المساعدة
│       ├── analytics/
│       │   └── kpiDefinitions.ts  # تعريفات مؤشرات الأداء
│       ├── insights/
│       │   └── ruleBasedInsights.ts  # رؤى قائمة على القواعد
│       ├── audit.ts            # تسجيل التدقيق
│       ├── design-system.ts    # نظام التصميم الموحد
│       ├── pdfGenerator.ts     # توليد ملفات PDF
│       └── supabase.ts        # إعداد Supabase
│
├── public/                     # الملفات الثابتة
│   ├── manifest.json           # PWA Manifest
│   ├── sw.js                   # Service Worker
│   └── logo.png
│
├── supabase-schema.sql         # مخطط قاعدة البيانات
├── package.json
├── tsconfig.json
├── next.config.js
└── README.md
```

---

## 🗄️ قاعدة البيانات (Database Schema)

### الجداول الرئيسية

#### 1. `health_centers` - المراكز الصحية
```sql
- id (UUID, Primary Key)
- name (TEXT, Unique) - اسم المركز
- created_at, updated_at
```
**البيانات**: 20 مركز صحي افتراضي (مركز صحي الحويجة، الرشيد، إلخ)

#### 2. `profiles` - ملفات المستخدمين
```sql
- id (UUID, Primary Key, References auth.users)
- email (TEXT)
- full_name (TEXT) - الاسم الكامل
- health_center_id (UUID, References health_centers)
- health_center_name (TEXT) - اسم المركز
- role (TEXT) - 'admin' أو 'center_user'
- is_approved (BOOLEAN) - حالة الموافقة
- created_at, updated_at
```
**Row Level Security**: المستخدمون يرون ملفاتهم فقط، الإداريون يرون الجميع

#### 3. `monthly_statistics` - الإحصائيات الشهرية
```sql
- id (UUID, Primary Key)
- health_center_id (UUID, References health_centers)
- health_center_name (TEXT)
- user_id (UUID, References auth.users)
- month (TEXT) - رقم الشهر (01-12)
- year (INTEGER)
- statistics_data (JSONB) - البيانات الإحصائية الكاملة
- status (TEXT) - 'draft', 'submitted', 'approved', 'rejected'
- approved_by (UUID, References auth.users)
- approved_at (TIMESTAMP)
- rejection_reason (TEXT)
- created_at, updated_at
- UNIQUE (health_center_id, health_center_name, month, year)
```
**Row Level Security**:
- المستخدمون: إدراج وتحديث تقاريرهم فقط (إذا كانت draft أو rejected)
- الإداريون: قراءة وتحديث جميع التقارير (للموافقة/الرفض)

#### 4. `audit_logs` - سجل التدقيق
```sql
- id (UUID, Primary Key)
- user_id (UUID, References auth.users)
- action (TEXT) - نوع الإجراء
- target_type (TEXT) - نوع الهدف
- target_id (UUID) - معرف الهدف
- details (JSONB) - تفاصيل إضافية
- timestamp (TIMESTAMP)
```
**Row Level Security**: الإداريون فقط يمكنهم قراءة سجل التدقيق

### العلاقات (Relationships)
- `profiles` → `health_centers` (Many-to-One)
- `monthly_statistics` → `health_centers` (Many-to-One)
- `monthly_statistics` → `profiles` (Many-to-One, للمستخدم)
- `monthly_statistics` → `profiles` (Many-to-One, للموافق)

---

## 🔐 نظام المصادقة والأمان (Authentication & Security)

### المصادقة (Authentication)
- **المزود**: Supabase Auth
- **الطريقة**: Email/Password
- **التسجيل**: 
  - إنشاء حساب في `auth.users`
  - إنشاء ملف تلقائي في `profiles` (Trigger)
  - الحالة الافتراضية: `is_approved = false`

### الأذونات (Authorization)
- **الأدوار (Roles)**:
  - `admin`: وصول كامل (لوحة القطاع، الموافقات، سجل التدقيق)
  - `center_user`: وصول محدود (إرسال الإحصائيات، متابعة الحالة)

- **حماية الصفحات**: `ProtectedRoute` component
  - يتحقق من تسجيل الدخول
  - يتحقق من الدور المطلوب
  - يتحقق من حالة الموافقة (للمستخدمين الجدد)

### Row Level Security (RLS)
- جميع الجداول محمية بـ RLS
- السياسات (Policies) محددة لكل جدول حسب الدور

---

## 📊 الميزات الرئيسية (Core Features)

### 1. إرسال الإحصائيات الشهرية (`/statistics`)
**للمستخدمين (center_user)**

- **رفع ملف Excel**: المستخدم يرفع ملف Excel يحتوي على البيانات الإحصائية
- **المعالجة**: 
  - قراءة الملف باستخدام `xlsx`
  - استخراج البيانات
  - حفظها في `monthly_statistics` كـ JSONB
- **حالات التقرير**:
  - `draft`: مسودة (قبل الإرسال)
  - `submitted`: قيد المراجعة (بعد الإرسال)
  - `approved`: معتمد (بعد موافقة الإدارة)
  - `rejected`: مرفوض (مع سبب الرفض)
- **القيود**:
  - لا يمكن تعديل تقرير معتمد
  - يمكن تعديل تقرير مرفوض بعد إصلاح المشاكل
- **تصدير PDF**: بعد الاعتماد، يمكن تحميل PDF رسمي

### 2. لوحة متابعة القطاع (`/sector-dashboard`)
**للإداريين (admin)**

- **عرض جميع المراكز**: قائمة بجميع المراكز الصحية (20 مركز)
- **حالات الإرسال**: 
  - معتمد / قيد المراجعة / مرفوض / لم يرسل
- **التحليلات**:
  - إجمالي النشاط لكل مركز
  - ترتيب المراكز (أعلى/أقل أداء)
  - تحليل النشاط حسب الفئات
  - رسوم بيانية (Bar Charts, Pie Charts)
- **الموافقة/الرفض**:
  - الإداري يمكنه الموافقة أو الرفض
  - إضافة سبب الرفض (مطلوب)
  - تسجيل في `audit_logs`
- **التصدير**: تصدير Excel شامل مع رأس رسمي

### 3. إنشاء البوسترات التوعوية (`/poster`)
**لجميع المستخدمين**

- **Wizard متعدد الخطوات**:
  1. اختيار الموضوع (من قائمة محددة مسبقاً)
  2. تعديل المحتوى (العنوان والرسالة)
  3. المعاينة
  4. التصدير
- **المواضيع المتاحة**: 
  - الأمراض المعدية
  - الأمراض غير المعدية
  - صحة الأم والطفل
  - التثقيف الغذائي
  - إلخ...
- **التصدير**:
  - PNG (للمشاركة على وسائل التواصل)
  - PDF (للطباعة الرسمية)
- **التقنيات**: `html2canvas` + `html-to-image` + `jspdf`

### 4. الموافقات (`/admin/approvals`)
**للإداريين**

- **قائمة التقارير المعلقة**: جميع التقارير بحالة `submitted`
- **الموافقة/الرفض**:
  - زر موافقة → تحديث `status = 'approved'`
  - زر رفض → إدخال سبب → تحديث `status = 'rejected'`
- **التفاصيل**: عرض البيانات الإحصائية الكاملة

### 5. سجل التدقيق (`/admin/audit-log`)
**للإداريين فقط**

- **عرض جميع الإجراءات**:
  - تسجيل الدخول
  - إرسال تقرير
  - موافقة/رفض تقرير
  - إنشاء PDF
  - إلخ...
- **التفاصيل**: المستخدم، الوقت، نوع الإجراء، التفاصيل

### 6. التقارير (`/admin/reports`)
**للإداريين**

- عرض جميع التقارير مع إمكانية البحث والفلترة

### 7. دليل المستخدم (`/admin/user-guide`)
**لجميع المستخدمين**

- **PDF ديناميكي**: توليد PDF يحتوي على:
  - خطوات تسجيل الدخول
  - خطوات إرسال الإحصائيات
  - متابعة حالة التقرير
  - نصائح مهمة
- **التقنية**: `jspdf` مع دعم RTL

### 8. العرض التقديمي التنفيذي (`/admin/presentation`)
**للإداريين فقط**

- **PDF ديناميكي**: توليد PDF يحتوي على:
  - ملخص تنفيذي
  - مؤشرات رئيسية
  - حالة التقارير
  - ملخص النشاط
- **البيانات**: مأخوذة من قاعدة البيانات مباشرة

---

## 🎨 نظام التصميم (Design System)

### الملف: `src/lib/design-system.ts`

**الألوان**:
- Primary: Emerald (أخضر) - `#059669`
- Success: Green - `#10b981`
- Warning: Yellow - `#f59e0b`
- Danger: Red - `#ef4444`
- Neutral: Gray - `#6b7280`

**الحالات (Status)**:
- `draft`: مسودة (رمادي)
- `submitted`: قيد المراجعة (أصفر)
- `approved`: موافق عليه (أخضر)
- `rejected`: مرفوض (أحمر)

**المكونات الموحدة**:
- `StatusBadge`: شارة الحالة (مصدر واحد للحقيقة)
- `PageContainer`: حاوية الصفحة
- `PageHeader`: رأس الصفحة
- `SectionCard`: بطاقة القسم

---

## 📱 Progressive Web App (PWA)

### الملفات
- `public/manifest.json`: تعريف التطبيق
- `public/sw.js`: Service Worker
- `src/components/PWAInstallPrompt.tsx`: مطالبة التثبيت

### الميزات
- ✅ قابل للتثبيت على الموبايل
- ✅ يعمل بدون إنترنت (Offline)
- ✅ تجربة تشبه التطبيق الأصلي
- ✅ أيقونة على الشاشة الرئيسية

---

## 🧠 الرؤى الذكية (Intelligent Insights)

### الملف: `src/lib/insights/ruleBasedInsights.ts`

**القواعد**:
1. **التقارير المفقودة**: اكتشاف المراكز التي لم ترسل تقاريرها
2. **الانخفاض المفاجئ**: اكتشاف انخفاض كبير في النشاط
3. **الزيادة المفاجئة**: اكتشاف زيادة غير طبيعية
4. **المراكز غير النشطة**: اكتشاف المراكز التي لم ترسل تقارير لعدة أشهر
5. **عدم التوازن في الفئات**: اكتشاف فئات مهملة

**المكون**: `InsightsPanel.tsx` يعرض الرؤى مع توصيات

---

## 📈 التحليلات (Analytics)

### الملف: `src/lib/analytics/kpiDefinitions.ts`

**المؤشرات**:
- إجمالي التقارير
- معدل الموافقة
- معدل الإرسال
- مقاييس النشاط
- التجميع على مستوى المركز والقطاع

**جاهزية**: البنية جاهزة للتحليلات المتقدمة في المرحلة الثانية

---

## 🔄 سير العمل (Workflow)

### للمستخدم (center_user)
1. **التسجيل**: إنشاء حساب → انتظار الموافقة
2. **تسجيل الدخول**: بعد الموافقة
3. **إرسال التقرير**:
   - اختيار الشهر
   - رفع ملف Excel
   - التقرير يصبح `submitted`
4. **متابعة الحالة**: 
   - `submitted` → قيد المراجعة
   - `approved` → يمكن تحميل PDF
   - `rejected` → قراءة سبب الرفض → إصلاح → إعادة الإرسال

### للإداري (admin)
1. **تسجيل الدخول**: وصول مباشر
2. **لوحة القطاع**: عرض جميع المراكز وحالاتها
3. **الموافقات**: 
   - عرض التقارير المعلقة
   - مراجعة البيانات
   - الموافقة أو الرفض (مع سبب)
4. **سجل التدقيق**: مراجعة جميع الإجراءات

---

## 🚀 النشر (Deployment)

### البيئة
- **المنصة**: Vercel (مُوصى به)
- **قاعدة البيانات**: Supabase Cloud
- **المتغيرات البيئية**:
  - `NEXT_PUBLIC_SUPABASE_URL`
  - `NEXT_PUBLIC_SUPABASE_ANON_KEY`

### البناء
```bash
npm run build
npm run start
```

### التحقق
- ✅ البناء ينجح بدون أخطاء
- ✅ لا توجد أخطاء TypeScript
- ✅ جميع الصفحات قابلة للوصول
- ✅ PWA قابل للتثبيت
- ✅ PDFs تُولد بشكل صحيح

---

## ✅ ما تم إنجازه (Completed Features)

### المرحلة الأولى (Phase 1) ✅
1. ✅ نظام المصادقة الكامل
2. ✅ إرسال الإحصائيات (رفع Excel)
3. ✅ لوحة متابعة القطاع
4. ✅ نظام الموافقات/الرفض
5. ✅ سجل التدقيق
6. ✅ إنشاء البوسترات التوعوية
7. ✅ توليد PDF (دليل المستخدم + عرض تقديمي)
8. ✅ PWA (Progressive Web App)
9. ✅ نظام التصميم الموحد
10. ✅ الرؤى الذكية (Rule-based)
11. ✅ Mobile-First Design
12. ✅ RTL Support (دعم العربية)

---

## 📋 المرحلة الثانية (Phase 2) - مخطط

### الملف: `PHASE2_PLANNING.md`

**الأهداف**:
1. **Advanced Analytics**: تحليلات متقدمة (Trend Analysis, Predictive Analytics)
2. **AI-Powered Insights**: رؤى مدعومة بالذكاء الاصطناعي
3. **Enhanced Reporting**: تقارير مخصصة ومجدولة
4. **Data Visualization**: رسوم بيانية تفاعلية متقدمة

**الحالة**: 📝 مخطط ومُوثق (لم يبدأ التنفيذ بعد)

---

## 🔧 التكوين المطلوب (Configuration)

### متغيرات البيئة (.env.local)
```env
NEXT_PUBLIC_SUPABASE_URL=your_supabase_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### قاعدة البيانات
1. إنشاء مشروع Supabase
2. تشغيل `supabase-schema.sql` في SQL Editor
3. تفعيل Row Level Security
4. إضافة المراكز الصحية الافتراضية

---

## 📚 التوثيق (Documentation)

### الملفات المتوفرة
- `README.md`: دليل البدء السريع
- `ECOSYSTEM_ARCHITECTURE.md`: هيكل النظام البيئي
- `PHASE2_PLANNING.md`: تخطيط المرحلة الثانية
- `VERIFICATION_REPORT.md`: تقرير التحقق من الميزات
- `PROJECT_SUMMARY_AR.md`: هذا الملف (ملخص للمطورين)

---

## 🎯 نقاط القوة (Strengths)

1. **Architecture**: بنية نظيفة ومنظمة
2. **Security**: أمان قوي (RLS, Protected Routes)
3. **UX**: تجربة مستخدم ممتازة (Mobile-First, RTL)
4. **Scalability**: قابل للتوسع (جاهز للمرحلة الثانية)
5. **Documentation**: توثيق شامل
6. **Code Quality**: كود نظيف وموثق

---

## 🔮 المستقبل (Future)

### المرحلة الثانية
- تحليلات متقدمة
- ذكاء اصطناعي
- تقارير مخصصة
- رسوم بيانية تفاعلية

### التحسينات المحتملة
- إشعارات (Notifications)
- تصدير متعدد الصيغ
- API للأنظمة الخارجية
- تطبيق موبايل أصلي (React Native)

---

## 📞 معلومات الاتصال

**المشروع**: Health Poster Legend  
**النوع**: Digital Health Platform  
**الحالة**: ✅ Production Ready  
**الإصدار**: 1.0.0

---

**تم إنشاء هذا الملخص لمساعدة شريك AI (Gemini) على فهم المشروع بالكامل** 🤖

